<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Tarea3" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Color de la status bar -->
        <item name="android:statusBarColor">@color/pink</item>
        <!-- Opcional: Hace que la status bar sea transparente/translúcida -->
        <item name="android:windowTranslucentStatus">false</item>
        <!-- Color del texto/íconos de la status bar (claro u oscuro) -->
        <item name="android:windowLightStatusBar">true</item>  <!-- Íconos oscuros -->
    </style>

    <style name="Theme.Tarea3" parent="Base.Theme.Tarea3" />
</resources>